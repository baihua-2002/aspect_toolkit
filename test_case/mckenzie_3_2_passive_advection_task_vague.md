# 用例 3.2（模糊版）：被动孔隙度平流

## 需求

我想看孔隙度当一个"被动示踪剂"被热对流驱动的流场搬着走会怎样。2D 无量纲盒子里，底热顶冷，希望出现对流胞。你帮我用 ASPECT 搭好。

## 请你处理

数值细节我没给，也不会给——这正是要考验你的地方。请你：

1. 结合该问题在文献里的标准做法（必要时检索 RAG 案例库 / ASPECT 参数定义），把盒子尺寸、网格、粘度、密度、重力、热膨胀、边界条件、初始温度、求解器格式、后处理等**全部参数**定出来；
2. 对每一个"我没有明确告诉你、只能靠你假设补齐"的参数，明确写出来：**它是什么、你给了什么值、依据是什么**（ASPECT 默认值 / 文献惯例 / 合理假设）；
3. 最终产出一个能跑通的 `.prm` 文件，并实际运行验证。

## 期望结果（提示，不是标准答案）

底热顶冷驱动热浮力对流，形成对流胞；孔隙度场被流场被动搬运，看不出明显的数值扩散失真。
