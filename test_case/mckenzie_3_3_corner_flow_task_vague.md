# 用例 3.3（模糊版）：角流与熔体聚焦

## 需求

我想复现洋中脊下方的角流（corner flow），看看动态压力梯度会不会把熔体往洋脊轴汇聚。2D 最简模型就行，你帮我用 ASPECT 搭。

## 请你处理

数值细节我没给，也不会给——这正是要考验你的地方。请你：

1. 结合该问题在文献里的标准做法（必要时检索 RAG 案例库 / ASPECT 参数定义），把盒子尺寸、网格、粘度、密度、重力、边界条件（特别是如何用边界速度制造"角流"）、温度求解、求解器格式、后处理等**全部参数**定出来；
2. 对每一个"我没有明确告诉你、只能靠你假设补齐"的参数，明确写出来：**它是什么、你给了什么值、依据是什么**（ASPECT 默认值 / 文献惯例 / 合理假设）；
3. 最终产出一个能跑通的 `.prm` 文件，并实际运行验证。

## 期望结果（提示，不是标准答案）

流场呈角流形态（一边水平拖拽、一边竖直下沉的角点结构），压力在角点附近有奇异性。
